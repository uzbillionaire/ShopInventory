# node_modules must be installed inside the image: your Mac's copies contain
# binaries compiled for macOS that cannot run on the Linux container.
node_modules/

# The build happens inside the image; a stale local dist would only confuse it.
dist/

.env
.env.*
.DS_Store
